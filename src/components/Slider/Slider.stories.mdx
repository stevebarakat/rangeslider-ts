import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Slider from "./Slider";

  <Meta title="Slider" component={Slider} />

export const Template = ({ colorPrimary, colorSecondary, ...args }) => <Slider style={{ '--color-primary': colorPrimary, '--color-secondary': colorSecondary }} {...args} />;

# Slider

## A simple Slider component.

<p className="mdp">Yada yada yada...</p>

<Preview>
  <Story
    name="Primary"
    args={{
      text: "Slider",
      colorPrimary: "green",
      colorSecondary: "lime"
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story="Primary" />
