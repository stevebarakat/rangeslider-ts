import { ArgsTable } from "@storybook/addon-docs";
import { useArgTypes } from "@storybook/api";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { RangeSlider } from "./RangeSlider";

<Meta title="RangeSlider" component={RangeSlider} />

export const Template = ({ focusColor, ...args }) => (
  <RangeSlider style={{ "--color-primary": focusColor }} {...args} />
);

# RangeSlider

## A single value horizontal range slider.

<p className="mdp">
  A control for entering a number whose exact value is not important. Displays
  as a range widget defaulting to the middle value. Used in conjunction{" "}
  <code>min</code> and <code>max</code> to define the range of acceptable
  values.
</p>

<Preview>
  <Story
    name="Primary"
    args={{
      initialValue: 50,
      step: 5,
      snap: true,
      showTicks: true,
      showLabels: true,
      showTooltip: true,
      rotateLabels: false,
      customLabels: [],
      prefix: "",
      suffix: "",
      width: 950,
      wideTrack: true,
      focusColor: "black",
      blurColor: "grey" 
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story="Primary" />
