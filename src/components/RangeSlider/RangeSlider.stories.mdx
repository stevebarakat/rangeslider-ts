import { ArgsTable } from "@storybook/addon-docs";
import { useArgTypes } from "@storybook/api";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { RangeSlider } from "./RangeSlider";

<Meta title="RangeSlider" component={RangeSlider} />

export const ShortStory = (args) => <div className="short-story"><RangeSlider {...args} /></div>;
export const MidStory = (args) => <div className="mid-story"><RangeSlider {...args} /></div>;
export const TallStory = (args) => <div className="tall-story"><RangeSlider {...args} /></div>;

# RangeSlider

## A single value horizontal range slider.

<p className="mdp">
  A control for entering a number whose exact value is not important. Displays
  as a range widget defaulting to the middle value. Used in conjunction{" "}
  <code>min</code> and <code>max</code> to define the range of acceptable
  values.
</p>

<Preview>
  <Story
    name="One"
    args={{
      initialValue: 50,
      step: 5,
      snap: true,
      showTicks: true,
      showLabel: true,
      showTooltip: true,
      rotateLabel: false,
      customLabels: [],
      prefix: "",
      suffix: "",
      decimals: 0,
      width: 950,
      wideTrack: true,
    }}
  >
    {TallStory.bind({})}
  </Story>
</Preview>

<Props story="One" />

| <h3>Default Settings</h3>                                                       | <h3>Default Settings</h3>                                                       | <h3>Default Settings</h3>                                                       |
| :------------------------------------------------------------------------------ | :------------------------------------------------------------------------------ | :------------------------------------------------------------------------------ |
| The most basic configuration. These are the values used if not set by the user. | The most basic configuration. These are the values used if not set by the user. | The most basic configuration. These are the values used if not set by the user. |

<Con className="flex" style={{height: "240px"}}>

  <Preview>
  <Story
    name="1.1"
    args={{
      initialValue: 50,
      width: 305,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>

<Preview>
  <Story
    name="1.2"
    args={{
      initialValue: 50,
      step: 10,
      snap: true,
      showTicks: true,
      showLabel: false,
      showTooltip: false,
      rotateLabel: false,
      customLabels: [],
      prefix: "",
      suffix: "",
      decimals: 0,
      width: 305,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>


<Preview>
  <Story
    name="1.3"
    args={{
      initialValue: 50,
      step: 10,
      snap: true,
      showTicks: true,
      showLabel: true,
      showTooltip: false,
      rotateLabel: false,
      customLabels: [],
      prefix: "",
      suffix: "",
      decimals: 0,
      width: 305,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>


</Con>

| <h3>Default Settings</h3>                                                         | <h3>Default Settings</h3>                                                                  
| :------------------------------------------------------------------------------ | :------------------------------------------------------------------------------ | 
| The most basic configuration. These are the values used if not set by the user. | The most basic configuration. These are the values used if not set by the user. | 

<Con className="flex">

  <Preview>
  <Story
    name="2.1"
    args={{
      initialValue: 50,
      width: 480,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>

<Preview>
  <Story
    name="2.2"
    args={{
      initialValue: 50,
      width: 480,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>

</Con>

### Configuration Two

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Two"
    args={{
      initialValue: 50,
      step: 5,
      snap: true,
      showTicks: true,
      showLabel: true,
      showTooltip: true,
      rotateLabel: false,
      customLabels: [],
      prefix: "",
      suffix: "",
      decimals: 0,
      width: 950,
    }}
  >
    {TallStory.bind({})}
  </Story>
</Preview>

### Configuration Three

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Three"
    args={{
      initialValue: 14,
      min: 5,
      max: 15,
      step: 1,
      snap: false,
      showTicks: true,
      showLabel: true,
      showTooltip: true,
      rotateLabel: false,
      customLabels: [],
      prefix: "$",
      suffix: " trillion",
      decimals: 0,
      width: 950,
      wideTrack: true,
    }}
  >
    {TallStory.bind({})}
  </Story>
</Preview>

### Configuration Four

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Four"
    args={{
      initialValue: 50,
      min: 0,
      max: 84,
      decimals: 0,
      step: 7,
      showTicks: true,
      snap: true,
      customLabels: [],
      showLabel: true,
      prefix: "",
      suffix: "",
      rotateLabel: false,
      width: 950,
      wideTrack: true,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>
