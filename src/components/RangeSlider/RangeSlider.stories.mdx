import { ArgsTable } from "@storybook/addon-docs";
import { useArgTypes } from "@storybook/api";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { RangeSlider } from "./RangeSlider";

<Meta
  title="RangeSlider"
  component={RangeSlider}
/>

export const Template = (args) => <RangeSlider {...args} />;

# RangeSlider

## A single value horizontal range slider.

<p className="mdp">
  A control for entering a number whose exact value is not important. Displays
  as a range widget defaulting to the middle value. Used in conjunction{" "}
  <code>min</code> and <code>max</code> to define the range of acceptable
  values.
</p>


<Preview>
  <Story
    name="Default: Thick"
    args={{
      initialValue: 50 
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

### Default Configuration

<p className="mdp">
  The most basic configuration. These are the values used if not set by
  the user.
</p>

<Props story="Default: Thick" />

#### Configuration Two

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Two"
    args={{
      initialValue: 50,
      min: 0,
      max: 100,
      decimals: 0,
      step: 5,
      showTicks: true,
      snap: true,
      customLabels: [],
      showLabel: false,
      prefix: "",
      suffix: "",
      rotateLabel: false,
      blurColor: "grey",
      focusColor: "black",
      width: 950,
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story="Two" />

#### Configuration Three

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Three"
    args={{
      initialValue: 50,
      min: 0,
      max: 100,
      decimals: 0,
      step: 5,
      showTicks: true,
      snap: true,
      customLabels: [],
      showLabel: false,
      prefix: "",
      suffix: "",
      rotateLabel: false,
      blurColor: "grey",
      focusColor: "black",
      width: 950,
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story="Three" />

#### Configuration Four

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Four"
    args={{
      initialValue: 50,
      min: 0,
      max: 100,
      decimals: 0,
      step: 5,
      showTicks: true,
      snap: true,
      customLabels: [],
      showLabel: false,
      prefix: "",
      suffix: "",
      rotateLabel: false,
      blurColor: "grey",
      focusColor: "black",
      width: 950,
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story="Four" />
