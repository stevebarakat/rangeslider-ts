import { ArgsTable } from "@storybook/addon-docs";
import { useArgTypes } from "@storybook/api";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { RangeSlider } from "./RangeSlider";

<Meta title="RangeSlider" component={RangeSlider} />

export const ShortStory = (args) => (
  <div className="short-story">
    <RangeSlider {...args} />
  </div>
);
export const MidStory = (args) => (
  <div className="mid-story">
    <RangeSlider {...args} />
  </div>
);
export const TallStory = (args) => (
  <div className="tall-story">
    <RangeSlider {...args} />
  </div>
);

# RangeSlider

## A single value horizontal range slider.

<p className="mdp">
  A control for entering a number whose exact value is not important. Displays
  as a range widget defaulting to the middle value. Used in conjunction{" "}
  <code>min</code> and <code>max</code> to define the range of acceptable
  values.
</p>

<Preview>
  <Story
    name="Primary"
    args={{
      initialValue: 50,
      step: 5,
      snap: true,
      showTicks: true,
      showLabels: true,
      showTooltip: true,
      rotateLabel: false,
      customLabels: [],
      prefix: "",
      suffix: "",
      decimals: 0,
      width: 950,
      wideTrack: true,
    }}
  >
    {TallStory.bind({})}
  </Story>
</Preview>

<Props story="Primary" />

<Con className="flex">
  <div>
    <h3>Default</h3>
    <p>
      The most basic configuration. These are the values used if not set by the
      user.
    </p>
  </div>
  <div>
    <h3>Ticks</h3>
    <p>
      The most basic configuration. These are the values used if not set by the
      user.
    </p>
  </div>
  <div>
    <h3>Ticks & Labels</h3>
    <p>
      The most basic configuration. These are the values used if not set by the
      user.
    </p>
  </div>
</Con>

<Con className="flex">

<Preview>
  <Story
    name="Default"
    args={{
      initialValue: 50,
      width: 305,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>

<Preview>
  <Story
    name="Ticks"
    args={{
      initialValue: 50,
      step: 10,
      showTicks: true,
      width: 305,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>

<Preview>
  <Story
    name="Ticks & Lables"
    args={{
      initialValue: 50,
      step: 10,
      showLabels: true,
      showTicks: true,
      width: 305,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>

</Con>

<Con className="flex">
  <div>
    <h3>ToolTip</h3>
    <p>
      The most basic configuration. These are the values used if not set by the
      user.
    </p>
  </div>
  <div>
    <h3>Snap & ToolTip</h3>
    <p>
      The most basic configuration. These are the values used if not set by the
      user.
    </p>
  </div>
</Con>

<Con className="flex">

<Preview>
  <Story
    name="ToolTip"
    args={{
      initialValue: 50,
      step: 10,
      showLabels: true,
      showTicks: true,
      showTooltip: true,
      width: 480,
    }}
  >
    {MidStory.bind({})}
  </Story>
</Preview>

<Preview>
  <Story
    name="Snap & ToolTip"
    args={{
      initialValue: 50,
      step: 10,
      snap: true,
      showLabels: true,
      showTicks: true,
      showTooltip: true,
      width: 480,
    }}
  >
    {MidStory.bind({})}
  </Story>
</Preview>

</Con>

### Prefix & Suffix

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Two"
    args={{
      initialValue: 14,
      min: 5,
      max: 150,
      step: 15,
      snap: false,
      showTicks: true,
      showLabels: true,
      showTooltip: true,
      rotateLabel: false,
      customLabels: [],
      prefix: "$",
      suffix: " USD",
      decimals: 0,
      width: 950,
      wideTrack: true,
    }}
  >
    {TallStory.bind({})}
  </Story>
</Preview>

### Configuration Three

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Three"
    args={{
      initialValue: 50,
      min: 0,
      max: 100,
      step: 10,
      snap: true,
      customLabels: [
        { 0: "losssssssssssssw" }, { 50: "medium" }, { 100: "higgfdgdgdgdfgdgdgdh"} 
      ],
      showTicks: true,
      showLabels: true,
      showTooltip: true,
      rotateLabel: false,
      decimals: 2,
      width: 950,
      wideTrack: true,
    }}
  >
    {TallStory.bind({})}
  </Story>
</Preview>

### Configuration Four

Default labels, not rotated, snapping, 10 steps.

<Preview>
  <Story
    name="Four"
    args={{
      initialValue: 50,
      min: 0,
      max: 84,
      decimals: 0,
      step: 7,
      showTicks: true,
      snap: true,
      customLabels: [],
      showLabels: true,
      prefix: "",
      suffix: "",
      rotateLabel: false,
      width: 950,
      wideTrack: true,
    }}
  >
    {ShortStory.bind({})}
  </Story>
</Preview>
