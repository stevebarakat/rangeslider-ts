import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { VerticalRangeSlider } from "./VerticalRangeSlider";
import { Box } from "../../shared/styles/css-storybook"

<Meta
  title="VerticalRangeSlider"
  component={VerticalRangeSlider}
  parameters={{
    actions: {
      handles: ["input"],
    },
  }}
/>

export const Template = ({
  focusColor,
  blurColor,
  lightColor,
  darkColor,
  sliderWidth,
  sliderHeight,
  ...args
}) => (
  <VerticalRangeSlider
    style={{
      "--color-primary": focusColor,
      "--color-secondary": blurColor,
      "--color-light": lightColor,
      "--color-dark": darkColor,
      "--slider-width": sliderWidth,
    }}
    {...args}
  />
);

# VerticalRangeSlider

## A single value horizontal range slider.

<p className="mdp">
  A control for entering a number whose exact value is not important. Displays
  as a range widget defaulting to the middle value. Used in conjunction{" "}
  <code>min</code> and <code>max</code> to define the range of acceptable
  values.
</p>

<Canvas>
  <Story
    name="Primary"
    args={{
      initialValue: 60,
      step: 20,
      snap: true,
      showTicks: true,
      showLabels: true,
      showTooltip: true,
      rotateLabels: false,
      customLabels: [],
      prefix: "Prefix ",
      suffix: " Suffix",
      wideTrack: true,
      lightColor: "",
      darkColor: "",
      focusColor: "",
      blurColor: "",
      sliderWidth: "",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Primary" />

<Box className="grid3">
  <div>
    <h3>Default</h3>
    <p>
      These are the settings used if not set as props. Defaults to <code>1</code> <code>step</code> intervals. Set <code>snap: false</code> for <code>0.1</code> <code>step</code> intervals.
    </p>
  </div>
  <div>
    <h3>Ticks</h3>
    <p>
      Displaying <code>ticks</code> with <code>10</code> <code>step</code> intervals. <code>step</code> must be greater than <code>0</code> for ticks to show.
    </p>
  </div>
  <div>
    <h3>Snap</h3>
    <p>
      Displaying <code>ticks</code> with <code>20</code> <code>step</code> intervals. <code>initialValue</code> should be equal to one of the <code>step</code> values.
    </p>
  </div>

<Canvas>
  <Story name="Default" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Ticks"
    args={{
      step: 10,
      showTicks: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Snap"
    args={{
      initialValue: 60,
      step: 20,
      snap: true,
      showTicks: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

</Box>

<Box className="grid2">
  <div>
    <h3>ToolTip</h3>
    <p>
      Showing <code>toolTip</code> with default labels. Default labels start at <code>min</code> and end at <code>max</code> and have <code>step</code> length intervals.
    </p>
  </div>
  <div>
    <h3>Custom Labels</h3>
    <p>
      <code>customLabels</code> can be created at specific <code>step</code>s within the value range. To define <code>customLabels</code>, create an array of key/value pair objects. View the code below for an example. <span className="strong"><code>customLabels</code> must land on a <code>step</code></span> to be displayed.
    </p>
  </div>

<Canvas>
  <Story
    name="ToolTip"
    args={{
      step: 10,
      showLabels: true,
      showTicks: true,
      showTooltip: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Custom Labels"
    args={{
      step: 10,
      snap: true,
      showLabels: true,
      showTicks: true,
      showTooltip: true,
      customLabels: [{ 0: "low" }, { 50: "medium" }, { 100: "high" }],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

</Box>

<Box className="grid2">
  <div>
    <h3>Prefix</h3>
    <p>
      Display custom text before the default label value and <code>toolTip</code> value. <code>prefix</code> <span className="strong">will not</span> display on <code>customLabels</code>.
    </p>
  </div>
  <div>
    <h3>Suffix</h3>
    <p>
      Display custom text after the default label value and <code>toolTip</code> value. <code>suffix</code> <span className="strong"> will not</span> display on <code>customLabels</code>.
    </p>
  </div>

<Canvas>
  <Story
    name="Prefix"
    args={{
      initialValue: 40,
      snap: false,
      step: 20,
      showLabels: true,
      showTicks: true,
      showTooltip: true,
      prefix: "$",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Suffix"
    args={{
      initialValue: 40,
      step: 20,
      showLabels: true,
      showTicks: true,
      showTooltip: true,
      suffix: " lbs",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

</Box>

<Box className="grid2">
  <div>
    <h3>Decimals</h3>
    <p>
      To measure values less than <code>1</code>, set the <code>step</code> to a value less than one. <code>step: 0.1</code> for tenths. <code>step: 0.01</code> for hundredths, etc. Be careful using <code>showLabels</code> and <code>showTicks</code> with small intervals. <code>ticks</code> and <code>labels</code> above <code>100</code> will not be displayed.
    </p>
  </div>
  <div>
    <h3>Decimals w/o Labels</h3>
    <p>
      To measure values less than <code>1</code>, set the <code>step</code> to a value less than one. <code>step: 0.1</code> for tenths. <code>step: 0.01</code> for hundredths, etc. When using <code>steps</code> less than <code>1</code>, it's better not to <code>showLabels</code> and <code>showTicks</code>.
    </p>
  </div>

<Canvas>
  <Story
    name="Decimals"
    args={{
      initialValue: 1.12,
      min: 1,
      max: 1.24,
      step: 0.03,
      snap: false,
      customLabels: [],
      showTicks: true,
      showLabels: true,
      showTooltip: true,
      rotateLabels: false,
      wideTrack: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Decimals w/o Labels"
    args={{
      initialValue: 6.74,
      min: 1.24,
      max: 12.24,
      step: 0,
      snap: false,
      customLabels: [],
      showTicks: true,
      showLabels: false,
      showTooltip: true,
      rotateLabels: false,
      wideTrack: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

</Box>

<Box className="grid2">
  <div>
    <h3>Odd Numbering</h3>
    <p>
      <span className="strong">Important! </span>The distance between <code>max</code> and <code>min</code> (<code>max - min</code>) must be divisible by <code>step</code>! Using modulo operator: <code>max - min % step</code> must equal <code>0</code>. For example, this works: <code>(84 - 14) / 7 = 10</code>, this doesn't: <code>(100 - 0) / 7 = 14.3</code>
    </p>
  </div>
  <div>
    <h3>Rotated Labels w/ Custom Colors</h3>
    <p>
      Rotate labels 45 degrees to allow for longer / more labels. <br />
      '--color-dark': 'rebeccapurple',<br />
    '--color-light': '#FEFEFE',<br />
    '--color-primary': 'rebeccapurple',<br />
    '--color-secondary': 'hotpink',
    </p>
  </div>

<Canvas>
  <Story
    name="Odd Numbering"
    args={{
      initialValue: 42,
      min: 14,
      max: 84,
      step: 7,
      showTicks: true,
      snap: true,
      customLabels: [],
      showLabels: true,
      showTooltip: true,
      prefix: "",
      suffix: "",
      rotateLabels: false,
      wideTrack: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Roatated Labels"
    args={{
      initialValue: 6000000,
      min: 1000000,
      max: 10000000,
      step: 1000000,
      snap: false,
      customLabels: [],
      prefix: "$",
      showTicks: true,
      showLabels: true,
      showTooltip: true,
      rotateLabels: true,
      wideTrack: true,
      focusColor: "rebeccapurple",
      blurColor: "hotpink",
      lightColor: "#FEFEFE",
      darkColor: "rebeccapurple",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

</Box>
